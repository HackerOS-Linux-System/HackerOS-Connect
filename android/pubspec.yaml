name: hackeros_connect_mobile
description: HackerOS Connect Mobile
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  multicast_dns: ^0.3.3
  flutter_local_notifications: ^19.5.0
  clipboard: ^2.0.2
  file_picker: ^10.3.3
  device_info_plus: ^11.1.0
  battery_plus: ^6.0.0  # Added for real battery level

flutter:
  uses-material-design: true
  assets:
    - images/HackerOS-Connect.png  # Add your icon image here
